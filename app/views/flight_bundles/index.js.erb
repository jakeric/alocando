console.log('<%= @bundle_id %>')
function displayDestinationInfo(innerHTML) {
  const destinationInfo = document.getElementById('destination_infos');
  destinationInfo.innerHTML = innerHTML;
}

displayDestinationInfo('<%= j render "components/destination_card", flight_bundle: @bundle_id %>');
